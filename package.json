{
  "name": "re-pxl",
  "version": "1.0.0",
  "description": "Pixel-art animation-focused drawing application made with Gemini",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp -r styles dist/ && cp -r js dist/ && cp -r assets dist/ 2>/dev/null || true",
    "dev": "npx http-server"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
